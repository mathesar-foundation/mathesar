# Mathesar 0.10.0

## Summary

Mathesar 0.10.0 adds UI support for existing PostgreSQL `ENUM` columns, introduces a custom User data type for storing references to Mathesar users, and introduces lightweight attribution tracking for changes to records. Mathesar's UI is also now available in two new languages – Spanish and French.

This release also includes a few other bug fixes and maintainence upgrades, and removes official support for Python 3.9. Mathesar's official deployment methods now use Python 3.14.

!!! info ""
	This page provides a comprehensive list of all changes in the release.

## Improvements

### User data type

We now have a custom User data type for columns to be able to store references Mathesar users. You can use it for building workflows inside Mathesar, for fields such as assignee, owner, reviewer, or point of contact. 

Users in these columns can be displayed by username, display name, or email address, and selected from a searchable list when editing cells. User columns also support default values, including setting a specific default user for new rows. We do not currently support adding User columns to forms.

### Last editor tracking

You can designate a column with a user data type to automatically store the current Mathesar user whenever a record is added or updated through Mathesar’s UI or API. This is configured at the table level and disables the ability to manually edit the column.

### UI support for PostgreSQL `ENUM` columns

Previously, Mathesar's UI rendered PostgreSQL `ENUM` columns as text fields that were validated at the database level. Now, `ENUM` columns show dropdowns with all the possible choices to make data entry simpler and quicker.

*Related work:*

[#5227](https://github.com/mathesar-foundation/mathesar/pull/5227 "Enums support")

### Spanish and French UI

Mathesar’s UI is now fully translated into Spanish and French. Thank you [Axell Padilla](https://github.com/axellpadilla) for the Spanish translations and [Jérôme Perez](https://github.com/jeromeperez) for the French translations!

*Related work:*

[#5229](https://github.com/mathesar-foundation/mathesar/pull/5229 "Add Spanish and French translations")

### Configurable bind address

*Related work:*

[#5218](https://github.com/mathesar-foundation/mathesar/pull/5218 "Add configurable bind address for Mathesar")


<!-- ## Groundwork -->
<!-- (Use this section to list any incremental work done on still-incomplete changes) -->


## Bug fixes

- Fix record ordering for non-orderable PostgreSQL types [#5152](https://github.com/mathesar-foundation/mathesar/pull/5152 "Fix record ordering for non-orderable PostgreSQL types")


## Documentation

- Fix link to Matrix chat in README [#5230](https://github.com/mathesar-foundation/mathesar/pull/5230 "Fix link to Matrix chat in README")
- Fix docs CI builds [#5250](https://github.com/mathesar-foundation/mathesar/pull/5250 "Fix docs CI builds")


## Maintenance

- Update to Python 3.14, remove 3.9 and PG 13 from tests [#5249](https://github.com/mathesar-foundation/mathesar/pull/5249 "Update to Python 3.14, remove 3.9 and PG 13 from tests")
- Bump requests from 2.32.4 to 2.33.0 [#5231](https://github.com/mathesar-foundation/mathesar/pull/5231 "Bump requests from 2.32.4 to 2.33.0")
- Bump requests from 2.32.4 to 2.33.0 in /api_tests [#5232](https://github.com/mathesar-foundation/mathesar/pull/5232 "Bump requests from 2.32.4 to 2.33.0 in /api_tests")
- Bump cairosvg from 2.8.2 to 2.9.0 [#5225](https://github.com/mathesar-foundation/mathesar/pull/5225 "Bump cairosvg from 2.8.2 to 2.9.0")
- Bump black from 24.3.0 to 26.3.1 in /docs [#5224](https://github.com/mathesar-foundation/mathesar/pull/5224 "Bump black from 24.3.0 to 26.3.1 in /docs")
- Bump django from 4.2.28 to 4.2.29 [#5222](https://github.com/mathesar-foundation/mathesar/pull/5222 "Bump django from 4.2.28 to 4.2.29")
- Bump django from 4.2.27 to 4.2.28 [#5202](https://github.com/mathesar-foundation/mathesar/pull/5202 "Bump django from 4.2.27 to 4.2.28")
- Bump django-allauth from 65.9.0 to 65.14.1 [#5239](https://github.com/mathesar-foundation/mathesar/pull/5239 "Bump django-allauth from 65.9.0 to 65.14.1")
- Bump pillow from 11.3.0 to 12.1.1 [#5210](https://github.com/mathesar-foundation/mathesar/pull/5210 "Bump pillow from 11.3.0 to 12.1.1")

## Upgrading to 0.10.0 {:#upgrading}

!!! warning "PostgreSQL 13 and Python 3.9 no longer supported"
    Mathesar 0.8.0 ended official support for PostgreSQL 13 and Python 3.9. Please upgrade to PostgreSQL 14+ and Python 3.10+ before upgrading to Mathesar 0.10.0.

### For installations using Docker Compose

If you have a Docker compose installation, run the command below:

```
docker compose -f /etc/mathesar/docker-compose.yml up --pull always -d
```

!!! warning "Your installation directory may be different"
    You may need to change `/etc/mathesar/` in the command above if you chose to install Mathesar to a different directory.

### For direct installations of Mathesar on Linux, macOS, or WSL

Mathesar provides an install script that automates both fresh installs and upgrades for standalone (non-Docker) installations.

Follow the steps below to upgrade Mathesar:

1. Enter your installation directory into the box below and press <kbd>Enter</kbd> to personalize this guide:

    <input data-input-for="MATHESAR_INSTALL_DIR" aria-label="Your Mathesar installation directory"/>

    - Do _not_ include a trailing slash.
    - Do _not_ use any variables like `$HOME`.

2.  Go to your Mathesar installation directory.

    ```
    cd xMATHESAR_INSTALL_DIRx
    ```

    !!! note
        Your installation directory may be different from above if you used a different directory when installing Mathesar.

3. Download and run the install script for 0.10.0
    ```
    curl -sSfL https://github.com/mathesar-foundation/mathesar/releases/download/0.10.0/install.sh -o install.sh
    chmod +x install.sh

    ./install.sh .
    ```

4. Replace your gunicorn systemd service with a Mathesar systemd service

    1. Disable and stop the existing gunicorn service
        ```
        systemctl disable gunicorn.service
        systemctl stop gunicorn.service
        ```

    2. Follow the steps in [Run Mathesar as a systemd service](../administration/install-from-scratch.md#run-mathesar-as-a-systemd-service) from the installation guide

    3. Remove the gunicorn service file
        ```
        sudo rm /lib/systemd/system/gunicorn.service
        ```

5. Update your Caddyfile

    1. Use the configuration shown in [Install and configure Caddy](../administration/install-from-scratch.md#install-and-configure-caddy) in the installation guide, and update your Caddyfile accordingly

    2. Ensure that your domains are specified directly in the first line of the Caddyfile

    3. Restart your Caddy service
      ```
      systemctl restart caddy.service
      ```

!!! tip
    **If you're running Mathesar only on localhost and do not want it to run as a service**, you could:

    1. Remove the gunicorn and caddy services
    1. Start Mathesar manually with:
      ```
      mathesar run

<!-- ## Security fixes -->

<!-- ## Breaking changes -->
<!-- (This section lists any breaking changes to publicly exposed and documented machine interfaces to Mathesar such as the API or DB functions) -->
