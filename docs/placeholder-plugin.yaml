MATHESAR_INSTALL_DIR: /home/my_user_name/mathesar
# Deprecated. Exists in older files because I don't want to clutter up
# installation related PRs, currently https://github.com/mathesar-foundation/mathesar/pull/4399.
# TODO: Remove in a future PR.
MATHESAR_INSTALLATION_DIR: /home/my_user_name/mathesar
MATHESAR_PG_DIR: /var/lib/docker/volumes/mathesar_postgresql_data/_data
DOMAIN_NAME: localhost
