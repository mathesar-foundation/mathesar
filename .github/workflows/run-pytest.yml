name: Run Python tests
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build the stack
        run: docker-compose up -d
      - name: Run tests with pytest
        run: docker-compose exec mathesar_web_1 pytest
