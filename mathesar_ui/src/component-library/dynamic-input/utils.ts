import type { DynamicInputType, DynamicInputSelectElement } from './types';

export function generateSelectOptions(
  type: DynamicInputType,
  enumValues,
  options: DynamicInputSelectElement['options'],
) {
  return [];
}
